<!-- 導覽列 -->
    <nav class="navbar navbar-default nav-style navbar-fixed-top">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <% if current_user %>
          <%= link_to root_path do %>
            <div class="nav-icon">
              <i class="fas fa-location-arrow"></i>
            </div>
          <% end %>
          <%= link_to trips_path do %>
            <div class="nav-icon" >
              <i class="fas fa-gamepad"></i>
            </div>
          <% end %>
          <% if controller?("trips") && action?("show")%>
            <button class="btn trip-button" data-toggle="modal" data-target="#myModal"><%= @trip.name%></button>
          <%end%>


          <%= link_to user_path(current_user) do %>
            <div class="nav-icon pull-right" >
              <%= image_tag "point.png", style: "width:35px;height:35px;"%>
              <%= current_user.points%>
            </div>
          <% end %>
          <!--
          <% if controller?("users") && action?("show") %>
            <a class= "navbar-brand pull-right">Profile</a>
          <%end%>
        -->
          <%end%>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="pull-right text-right nav navbar-nav">
            <% if current_user %>
              <% if current_user.role == "admin" %>
                <li><%= link_to "後台", admin_trips_path%><li>
              <%end%>
            <li><%= link_to('登出', destroy_user_session_path, method: :delete) %></li>
            <li><%= link_to('修改密碼', edit_user_registration_path) %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>