<div class="container">
  <div class="jumbotron">
    <h1>!挑戰路線!</h1>
    <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Harum cum earum eveniet omnis nostrum corporis excepturi repellendus at, cupiditate ut.</p>
  </div>
  <div class="row">
    <div >
      <% @trips.each do |t| %>
        <div class="thumbnail card col-xs-12 col-md-4">
          <img src="..." alt="...">
          <div class="caption">
            <h3><%= link_to "#{t.name}",challenge_trip_path(t), method: :post %></h3>
            <p class="text-right">
              <% if t.is_challenged?(current_user) %> <!--不知為啥只check一個就變true-->
                <button class="btn btn-primary">Done</button>
              <%else%>
                <button class="btn btn-primary">Go</button>
              <% end %> 
            </p>
          </div>
        </div>
      <%end%>
    </div>
  </div>
</div>
</div>
